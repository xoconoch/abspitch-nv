{
  "name": "Absolute Pitch",
  "id": "abspitch-nv",
  "author": "Xoconoch",
  "version": "0.1.0",
  "description": "Replaces Navidrome's similar tracks/artists with Absolute Pitch API recommendations.",
  "config": {
    "schema": {
      "type": "object",
      "properties": {
        "apiUrl": {
          "type": "string",
          "title": "Absolute Pitch API URL",
          "description": "Base URL for the Absolute Pitch server.",
          "default": "http://localhost:8100"
        }
      }
    },
    "uiSchema": {
      "type": "VerticalLayout",
      "elements": [
        {
          "type": "Control",
          "scope": "#/properties/apiUrl"
        }
      ]
    }
  },
  "permissions": {
    "config": {
      "reason": "To read plugin configuration in Navidrome UI"
    },
    "http": {
      "reason": "To call Absolute Pitch API for similar tracks and artists",
      "requiredHosts": ["*"]
    }
  }
}
