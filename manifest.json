{
  "name": "Absolute Pitch",
  "id": "abspitch-nv",
  "author": "Xoconoch",
  "version": "0.2.4",
  "description": "Replaces Navidrome's similar tracks/artists with Absolute Pitch API recommendations.",
  "config": {
    "schema": {
      "type": "object",
      "properties": {
        "apiUrl": {
          "type": "string",
          "title": "Absolute Pitch API URL",
          "description": "Base URL for the Absolute Pitch server.",
          "default": "http://localhost:8100"
        },
        "similarityThreshold": {
          "type": "number",
          "title": "Similarity Threshold",
          "description": "Excludes recommendations with similarity at or above this threshold, as they are likely duplicate tracks.",
          "default": 0.98
        }
      }
    },
    "uiSchema": {
      "type": "VerticalLayout",
      "elements": [
        {
          "type": "Control",
          "scope": "#/properties/apiUrl"
        },
        {
          "type": "Control",
          "scope": "#/properties/similarityThreshold"
        }
      ]
    }
  },
  "permissions": {
    "config": {
      "reason": "To read plugin configuration in Navidrome UI"
    },
    "http": {
      "reason": "To call Absolute Pitch API for similar tracks and artists",
      "requiredHosts": ["*"]
    }
  }
}
